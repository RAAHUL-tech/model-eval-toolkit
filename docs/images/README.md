# Brand assets

- **`evalreport-logo.png`** — Package logo used in the [README](../../README.md) and on PyPI (via absolute GitHub `raw` URL in `README.md`).

To refresh the artwork, replace this file and keep the same filename so the README link stays valid.
